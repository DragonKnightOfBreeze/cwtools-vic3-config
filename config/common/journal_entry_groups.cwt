types = {
    type[journal_entry_group] = {
        path = "game/common/journal_entry_groups"
        localisation = {
            name = "$"
        }
        subtype[country] = {
            context = country
        }
        subtype[contextless] = {
            ## cardinality = 0..1
            context = none
        }
    }
}

journal_entry_group = {
    context = enum[journal_entry_group_context]

    ## cardinality = 0..1
    updates_strategic_region_stances = bool
}
